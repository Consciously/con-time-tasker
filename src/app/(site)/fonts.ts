import { Oxanium, Nunito } from 'next/font/google';

export const oxanium = Oxanium({
	subsets: ['latin'],
	display: 'swap',
	weight: ['200', '400', '600'],
});

export const nunito = Nunito({
	subsets: ['latin'],
	display: 'swap',
	weight: ['200', '400', '600'],
});
