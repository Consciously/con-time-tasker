export enum Priority {
	Low = 'low',
	Medium = 'medium',
	High = 'high',
}

export enum Category {
	Customer = 'customer',
	Own = 'own',
	Private = 'private',
}

export enum Status {
	NotStarted = 'Not Started',
	InProgress = 'In Progress',
	Completed = 'Completed',
}
